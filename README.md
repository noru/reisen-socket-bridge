# reisen-socket-bridge

A socket server listenning to port 5000. Also a simple web interface to control Reisen movement.

## Run
```bash
# 1. start ros-arduino-bridge on the host
# 2. in project folder
python setup.py install
python app.py
```
